import React from 'react';
import sample from '../sample.json';

const Home = () => {
    console.log(sample);
    return (
        <main>
            <h1>Home</h1>
        </main>
    );
};

export default Home;